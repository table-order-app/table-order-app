{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "VITE_PORT_TABLE",
    "VITE_PORT_STAFF",
    "VITE_PORT_KITCHEN",
    "VITE_PORT_ADMIN"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false
    }
  }
}
